订单整理管理系统 V7.4.7 分类图片懒加载优化版

核心调整：
1. 生成整理文档时，先完成订单分类汇总。
2. 只读取本次结果中实际出现的分类图片关系。
3. 新增 data/image_categories/ 分类图片分片缓存。
4. 保存主数据时自动同步分类图片分片，兼容旧 image_map 数据。
5. 未调用到的分类图片不会进入 ImageMatcher，不再参与图片匹配扫描。

注意：
首次保存/维护数据后会生成分类分片缓存。旧数据未生成缓存时，会自动从主库按分类过滤兜底。
