V6.2 加载界面修复说明

问题：
V6/V6.1 的管理系统加载窗口虽然出现，但数据读取仍在 Tkinter 主线程执行。
旧数据较大时，主线程被 load_data() 阻塞，导致加载窗口无响应、进度条不动、预览不刷新。

修复：
订单整理管理系统启动时改为后台线程读取数据。
主线程只负责显示加载界面。
后台读取完成后，再初始化主界面并展示数据预览。

效果：
- 加载窗口不会直接卡死。
- 进度条可以持续动画。
- 数据读取完成后显示模板、规则、档口、图片数量。
- 读取异常会弹窗提示，不会静默卡住。
