V7.1 加载窗口修复说明

修复内容：
1. 加载窗口允许关闭，可“取消并退出”。
2. 后台线程不再直接操作 Tkinter UI，改为队列通信。
3. 主线程每 200ms 轮询队列并刷新日志，降低卡死概率。
4. 超过 5/15/30/60/120 秒会提示仍在读取。
5. 初始化空数据也走同一套安全加载逻辑。

说明：
如果旧数据文件 system_data.enc 极大，首次读取仍可能耗时。
进入管理系统后，请执行：
系统维护 -> 整理/修复旧数据
把旧 image_base64 迁移为 data/images 图片文件。
